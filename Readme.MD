# Anion

A lightweight desktop launcher built with Electron — a browser-style start screen with quick access to your favorite sites, plus a taskbar for managing open app windows.


## prebuilt packages are already installable for Mac OS, and windows 



## Features

- Search bar that directs to a google search
- Quick-access shortcuts to common sites 
- Draggable, resizable floating windows for each opened site
- Taskbar with minimize/restore

## Getting Started

Install dependencies:


download the file and run it idk

## Building

Build for your current platform:


npm run build

Build for a specific platform:


npm run build -- --win
npm run build -- --mac
npm run build -- --linux


Or build all three at once (macOS only, requires Wine for the Windows target):


npm run build -- --mac --win --linux


Built installers are output to the `dist/` folder.

##license 

honestly I don't care what you do with it just im not legally responsible for whatever
